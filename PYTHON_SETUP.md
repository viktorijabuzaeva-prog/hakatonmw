# 🐍 Установка Python для Windows

## Вариант 1: Автоматическая установка (Рекомендуется)

1. **Запустите файл** `install_python.bat` (двойной клик)
2. Дождитесь загрузки установщика Python
3. В окне установщика:
   - ✅ **ОБЯЗАТЕЛЬНО поставьте галочку** "Add Python to PATH"
   - Нажмите "Install Now"
4. Дождитесь завершения установки
5. После установки запустите `install_and_run.bat`

## Вариант 2: Ручная установка

### Шаг 1: Скачайте Python

Откройте в браузере:
```
https://www.python.org/downloads/
```

Или прямая ссылка на Python 3.11.9:
```
https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe
```

### Шаг 2: Запустите установщик

1. Откройте скачанный файл `python-3.11.9-amd64.exe`
2. **ВАЖНО**: Поставьте галочку ✅ **"Add Python to PATH"** (внизу окна)
3. Нажмите **"Install Now"**
4. Дождитесь завершения установки
5. Нажмите "Close"

### Шаг 3: Проверьте установку

Откройте новую командную строку (Win + R → cmd) и введите:

```cmd
python --version
```

Должно показать: `Python 3.11.9` (или похожую версию)

Если команда не найдена:
- Перезапустите командную строку
- Или перезагрузите компьютер

### Шаг 4: Запустите проект

Дважды кликните на файл:
```
install_and_run.bat
```

## 🎯 Что будет дальше

После установки Python:

1. `install_and_run.bat` установит необходимые библиотеки
2. Запустится backend сервер
3. Откройте `frontend\index.html` в браузере
4. Начните анализировать транскрипты!

## ❓ Проблемы?

### "Python не найден" после установки
- Убедитесь, что при установке была поставлена галочка "Add Python to PATH"
- Перезапустите командную строку или компьютер
- Переустановите Python с правильными настройками

### "Отказано в доступе" при установке
- Запустите установщик от имени администратора
- Правый клик → "Запуск от имени администратора"

### Нужна более старая версия Python?
- Python 3.8, 3.9, 3.10, 3.11, 3.12 - все подойдут
- Рекомендуем 3.11 для стабильности

## 📞 Альтернативные варианты установки

### Microsoft Store (Windows 10/11)
1. Откройте Microsoft Store
2. Найдите "Python 3.11"
3. Нажмите "Установить"
4. PATH настроится автоматически

### Winget (Windows Package Manager)
```cmd
winget install Python.Python.3.11
```

---

**После установки Python запустите `install_and_run.bat` для продолжения!**
