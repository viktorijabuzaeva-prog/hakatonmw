[build]
  # Директория с файлами сайта
  publish = "frontend"
  
  # Команда сборки (не нужна для статического сайта)
  # command = ""

# Редиректы для SPA (если нужно)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
